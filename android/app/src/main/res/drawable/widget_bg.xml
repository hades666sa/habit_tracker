<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="16dp" />
    <solid android:color="@color/widget_background" /> <!-- Light theme default, dark mode handles itself via OS color values if configured, but we'll use a neutral grey-ish or transparent. We'll set it dynamically in code. -->
</shape>
